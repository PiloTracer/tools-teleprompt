# Front-end stack — REPLACE:PROJECT_NAME

> **Bootstrap:** Created by `@ui-bootstrap init` when missing. Customize pins; link from `.cursorrules` as `REPLACE:UI_TECH_STACK_DOC`.

**Updated:** YYYY-MM-DD

## Runtime

| Item | Value |
|------|-------|
| Framework | REPLACE:UI_FRAMEWORK (e.g. React 19 + Next.js 15) |
| Language | REPLACE:UI_LANGUAGE (e.g. TypeScript 5.x) |
| Styling | REPLACE:UI_STYLE_SYSTEM (e.g. Tailwind 4, CSS Modules) |
| Package manager | REPLACE:UI_PACKAGE_MANAGER |

## Tooling (must match `.cursorrules`)

| Check | Command |
|-------|---------|
| Unit tests | `REPLACE:UI_TEST` |
| Lint | `REPLACE:UI_LINT` |
| Typecheck | `REPLACE:UI_TYPECHECK` |
| Visual regression | `REPLACE:UI_VISUAL_TEST` |
| Accessibility | `REPLACE:UI_A11Y_TOOL` |

## Paths

| Item | Path |
|------|------|
| App root | `REPLACE:UI_APP_ROOT` |
| Components | `REPLACE:UI_COMPONENTS_DIR` |
| Tokens | `REPLACE:UI_TOKENS_FILE` |
| Storybook | REPLACE:UI_STORYBOOK_DIR or n/a |

## Docker (if used)

| Service | Workdir |
|---------|---------|
| `REPLACE:SERVICE_FRONTEND` | `REPLACE:FRONTEND_WORKDIR` |

## Design references

- Inputs: `.ai.ui/inputs/`
- Screen SPECs: `.work.ui/screens/`
