# Decisions (pointer)

**UI ADRs** default to [`.work.ui/decisions/`](../.work.ui/decisions/) at repo root.

Teams using Agent OS may instead use `.work/decisions/` with `ui-` filename prefix — record the choice in `HANDOFF_UI.md`.

Format: `YYYYMMDD-NNN-ui-<slug>.md`
