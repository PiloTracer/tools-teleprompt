# Design examples (reference only)

**Not binding specs.** Use [`INDEX.md`](INDEX.md) playbook + per-folder `manifest.md` rows.

**PNG files:** gitignored in this repo (size). **Text-only agents:** use manifest `extractedRules` / `primitives` columns. **Vision agents:** need local `examples/**/*.png` on disk.

**Add an example:** PNG in subfolder → full manifest row → update `INDEX.md` value matrix.
