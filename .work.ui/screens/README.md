# Screen SPECs

One folder per slug: `<slug>/YYYYMMDD-SCREEN-SPEC.md`

Create via `@ui-screen-spec create - <slug>` after **screen-spec-ready**.

Do not place screen SPECs in `.work/features/` (domain SPECs only).
