# MOD-06 / W3 — M8 Adaptive teleprompter security review

**Date:** 2026-05-23  
**Iteration:** M8 · mic VAD + read zone + meta parser  
**Reviewer:** implementation agent (W3 formal scope for M8-T11)

## AI change risk summary

- AI-assisted: yes
- Boundaries crossed: 1 — `frontend/src/prompter/adaptive/` (new module) + integration hooks in `Player.tsx`, `PlayerControls.tsx`, `Settings.tsx`, `Editor.tsx`, `markdown/render.ts`
- New cross-boundary deps: none from adaptive → pairing/api; Player → adaptive hooks only (`measured` — grep adaptive imports)
- Test isolation: ok — `vitest tests/adaptive/` (41 tests `measured`); `player.test.tsx` adaptive block (5); Playwright `adaptive-player.spec.ts` (4 `measured`)
- Human architectural review: optional — client-only feature; defaults off; SPEC I1–I2 enforced in code + tests
- Blast radius: Mic permission UX wrong → fixed-speed scroll still works (R3, R23). VAD mis-tuned → scroll pauses/resumes incorrectly; no data exfiltration path. Meta misclassification → scroll speed skew on mis-tagged lines; XSS unchanged (SanitizedHtml + tp-meta class only). Adaptive off → zero getUserMedia (I2, I5).

## Mic privacy (NFR-12, I1–I2)

| Area | Finding | Severity | Mitigation |
|------|---------|----------|------------|
| Audio exfiltration | No fetch/XHR/beacon in `prompter/adaptive/*` | ok | `adaptiveInvariants.test.ts` static scan |
| getUserMedia gate | `enabled` + `listen` both required | ok | `useVoiceActivity.test.ts` I2 cases |
| Adaptive off | Mic button hidden; no getUserMedia on play | ok | `player.test.tsx` + Playwright I5 |
| Permission denied | Non-blocking hint; fixed scroll | ok | R23 vitest + e2e |
| Settings copy | Device-local processing stated | ok | R24 settings.test |

## Scroll controller (FR-13, G7)

| Area | Finding | Severity | Mitigation |
|------|---------|----------|------------|
| Read zone band | 35–48% center ~42% | ok | `useAdaptiveScroll.test.ts` |
| VAD pause | Silence → rate 0 on spoken lines | ok | simulate + vitest |
| Meta 2× skim | 2× multiplier; no VAD wait on meta | ok | amendment 01; META_SCROLL_MULTIPLIER tests |
| Speed slider | Does not move during sync (R16) | ok | Player wiring — manual + unit rate resolver |
| Reduced motion | Respects prefers-reduced-motion in hook | ok | `resolveAdaptiveScrollRate` null path |

## Meta parser + markdown (R11–R13, NFR-03)

| Area | Finding | Severity | Mitigation |
|------|---------|----------|------------|
| XSS | Classification pre-render; blockquote class via sanitize | ok | markdown.test.tsx corpus |
| Whole-line paren ambiguity | `(Hello?)` → meta v1 | Low | Editor hint (M8-T8); documented in SPEC |
| Line-index map | Plain meta not DOM-only | ok | parseScriptLines + offset map |

## Recommendation

merge_with_conditions — reason: client-only adaptive module; I1/I2 tested; no new server surface.

## Conditions

- `@code-verify milestone` pass before `@code-implementation complete`
- Manual mic tuning on real hardware optional (VAD threshold assumption)
- Production deploy unchanged (no API changes in M8)
