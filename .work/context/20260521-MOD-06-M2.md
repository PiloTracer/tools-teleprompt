# MOD-06 — M2 markdown render pipeline

**Date:** 2026-05-21  
**Iteration:** M2 · markdown-render SPEC  
**Diff:** 7 new frontend files + `package.json`/`package-lock.json` (~300 LOC application code, `estimated`)

## AI change risk summary

- AI-assisted: yes
- Boundaries crossed: 1 — `frontend/src/markdown/` (markdown-render bounded context within prompter-ui SPA)
- New cross-boundary deps: npm `markdown-it`, `dompurify`; internal imports `render` → `sanitize` | `plain`, `SanitizedHtml` ← `SafeHtml` type only — no FE↔API coupling
- Test isolation: ok — command: `docker compose -f deploy/docker-compose.yml exec frontend sh -c "cd /app && npm test -- markdown"` (`measured` 18 tests pass, 12 XSS fixtures)
- Human architectural review: optional — reason: single frontend module; isolated vitest + XSS corpus; formal cross-model review waived to M6-T6 per HANDOFF W3 (`assumption`)
- Blast radius: If sanitization or the `SanitizedHtml` choke-point is bypassed in M3+, user-supplied script content could XSS the teleprompter in the browser. Impact is client-side only (no server script storage in this module); worst case is compromised display session on the victim device until reload. No API, Redis, or pairing data affected.

## Recommendation

merge_with_conditions — reason: high-risk XSS surface (FEATURE_STANDARD) with strong module-scoped tests; acceptable to proceed to M3 if M3 enforces `SanitizedHtml` only.

## Conditions if merge_with_conditions

- Must add tests: none for M2 (12-vector corpus present); M3 must add component tests proving preview/player never bind raw HTML
- Must split PR: n/a — cohesive M2 module
