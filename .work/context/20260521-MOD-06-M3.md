# MOD-06 — M3 prompter UI core

**Date:** 2026-05-21  
**Iteration:** M3 · prompter-ui SPEC  
**Diff:** 14 new/changed frontend application files in commit `9be925a` (~960 LOC M3 portion, `estimated`; bundled with M2 in same commit)

## AI change risk summary

- AI-assisted: yes
- Boundaries crossed: 2 — `frontend/src/prompter/` (+ `routes/`, `lib/i18n/` as prompter-ui SPA layers), `frontend/src/markdown/` (consumed via Preview choke-point only)
- New cross-boundary deps: `Preview` → `renderScript`, `SanitizedHtml`, `ScriptFormat` type; `storage` → `ScriptFormat` type import only (`markdown/types`); npm `react-router-dom` ^7; no FE↔API imports
- Test isolation: ok — command: `docker compose -f deploy/docker-compose.yml exec frontend sh -c "cd /app && npm test -- prompter"` (`measured` 7/7 pass); full FE suite `npm test` (`measured` 29/29 pass)
- Human architectural review: optional — reason: crosses markdown↔prompter contract but SanitizedHtml is the sole HTML bind path in Preview; prompter tests assert no `<script>` in preview DOM (R7); MOD-06 M2 conditions satisfied; formal cross-model review deferred to M6-T6 per HANDOFF W3 (`assumption`)
- Blast radius: If wrong, users see broken navigation, lost/restored script settings, or XSS in the preview pane when markdown format is selected. Impact is client-side only (localStorage/IndexedDB on the user's device); no API, Redis, relay, or server-side script storage affected. Worst XSS case compromises the teleprompter display session until reload; plain-text path escapes HTML per R7.

## Recommendation

merge_with_conditions — reason: intentional cross-boundary use of markdown-render is contract-bound and tested; proceed to `@code-implementation complete` after validation gates in NEXT pass.

## Conditions if merge_with_conditions

- Must add tests: M4 player must reuse `SanitizedHtml` only (same MOD-06 M2 carry-forward); no additional M3 tests required — R2/R7/R11 covered in `prompter.test.tsx`
- Must split PR: n/a — M3 cohesive with landed commit
