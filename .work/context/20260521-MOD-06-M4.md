# MOD-06 — M4 Player and PWA

**Date:** 2026-05-21  
**Iteration:** M4 · prompter-ui SPEC  
**Diff:** ~20 new/changed frontend files + `bin/e2e-offline.sh` (~1.4k LOC application/config, `estimated`; uncommitted on `main`)

## AI change risk summary

- AI-assisted: yes
- Boundaries crossed: 3 — `frontend/src/prompter/` (player + hooks), `frontend/src/pwa/` (SW registration UX), `frontend/src/markdown/` (Player consumes via `SanitizedHtml` only)
- New cross-boundary deps: `vite-plugin-pwa` + workbox (build-time); `@playwright/test` 1.51.0 (dev); `Player` → `renderScript` + `SanitizedHtml`; no FE↔API imports
- Test isolation: ok — command: `docker compose -f deploy/docker-compose.yml exec frontend sh -c "cd /app && npm test -- player"` (`measured` 19/19 pass); offline e2e: `bin/e2e-offline.sh` (`measured` 1/1 pass in Playwright `v1.51.0-noble` image)
- Human architectural review: optional — reason: client-only SPA; module-scoped vitest + Playwright offline; SW update UX is banner-only (R10); MOD-04 ops note below; formal cross-model review deferred to M6-T6 (W3, `assumption`)
- Blast radius: If wrong, users get broken scroll/controls, battery drain from wake lock leaks, or stale PWA shell until reload/dismiss. SW misconfiguration could cache a broken app shell across all users until cache bust (`estimated` ops impact: MOD-04). XSS risk remains bounded to `SanitizedHtml` choke-point in Player (same as M3). No server, Redis, or pairing data affected.

## Recommendation

merge_with_conditions — reason: strong module tests + offline e2e; proceed to `@code-implementation complete` after validation gates; manual Lighthouse PWA audit still open.

## Conditions if merge_with_conditions

- Must add tests: none for M4 vitest coverage; offline e2e present — cold-start offline navigation not covered (client-side routing only, `assumption`)
- Must split PR: n/a — cohesive M4 milestone
- Manual: Lighthouse PWA audit (NEXT validation step)
- Ops (MOD-04): document SW update banner + `skipWaiting`/`clientsClaim` in deploy notes before production push
